# direnv: enter the Nix dev shell on cd. Run `direnv allow` once after cloning.
# Requires nix with flakes enabled; see DEVELOPERS.md.
use flake

# Personal, uncommitted settings (AWS_PROFILE etc). Optional -- see DEVELOPERS.md.
source_env_if_exists .envrc.local
